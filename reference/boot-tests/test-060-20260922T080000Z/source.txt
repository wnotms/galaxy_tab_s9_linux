source_commit=6462158 ("pogo: leave the MCU completely alone while its application starts")
upstream_kernel_commit=a13c140cc289c0b7b3770bce5b3ad42ab35074aa
authorization=standing device-test authorisation recorded for tests 046-059 ("刷入测试" / "检查仓库变化，继续修复") plus this round's direction to keep testing on hardware
hypothesis=Two possibilities: (a) the application is alive but this driver's own early activity keeps it from answering - every poll window so far had side effects, because the first failure called pogo_recover_bus() (nine bit-banged clocks plus a STOP on SCL/SDA and a pinctrl switch of the controller's pins) and the failure path scanned every address; or (b) the application really is not running.  Test 057's silent-window result was never captured, so neither silence nor side-effect-free polling has ever been measured.
change=connect_work powers the MCU up with BOOT0 low (as in test 058, pogo_supply stays output-low), then waits 30 s without touching the bus, the pins, NRST or 0x51, and only then polls CHECK_VERSION with a window that itself has no side effects: no bus recovery, no pinctrl change, no reset, no bootloader access.  The recovery bit-bang and the bus scan now run only in the failure path, after both poll windows and the bootloader visit have failed.
predictions=1) if (a) holds, the log shows "leaving the MCU alone for 30000 ms", then "silent window over; connect line X" and "MCU application answered after N ms", with no bootloader visit at all; 2) if (b) holds, the same 60 s silent poll and bootloader visit as test 058, and the MCU is definitively not running its application under mainline.
images=out/boot-bundle-test060/boot.img ae45f2402e3418259adac7e3ee3e5ecb69ed67835636a903b478f8bfad6578e0; vendor_boot 8d76f912, init_boot 12b77d17, dtbo c17418be unchanged
started_utc=2026-09-22T08:00:00Z
