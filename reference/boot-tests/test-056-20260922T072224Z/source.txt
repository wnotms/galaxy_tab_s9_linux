source_commit=9c9808e ("pogo: power the MCU up after its pins, the way the stock driver does")
upstream_kernel_commit=a13c140cc289c0b7b3770bce5b3ad42ab35074aa
authorization=owner asked to continue the repair ("检查仓库变化，继续修复"), to reduce the wait between system switches, for the card's automatic mount to be off with only the serial console, and pointed at the official source in the parent folder ("可以参考上级文件夹中的官方源码")
hypothesis=The application never runs in mainline because the MCU powers up before its boot pins are driven.  Mainline drove gpio10 (the MCU's rail) high from the regulator's pinctrl default state at the fixed regulator's probe, i.e. before the pogo driver applied its pinctrl state and so before gpio12 (SWCLK/BOOT0) was driven low; if this part latches its boot source at power-on, it comes up in its system bootloader and no later NRST poke changes that, which is what tests 046-054 measured.  Stock does the opposite order: test 055 showed the stock stack reaching a working application without ever entering the bootloader (its firmware path aborts because keyboard_stm/stm32_gts9family.bin is missing), with the rail switched in stm32_interrupt_init() after the driver owns gpio12/gpio13.
change=the board DTS no longer drives gpio10 from pinctrl (the regulator owns the pin), and connect_work powers the MCU up in stock's order: SWCLK low, NRST asserted, rail on, NRST released; then the application is polled with no SWCLK dance, no GO and no reset loop.  The bootloader visit remains only as a fallback after the application fails to answer twice.
predictions=1) the log shows "MCU powered up with BOOT0 low and NRST released". 2) the application answers CHECK_VERSION inside the first poll seconds later, so the driver logs "MCU application already running" and the model/hw/firmware/mode line, and no bootloader visit happens. 3) if the hypothesis is wrong, the log shows the same 60 s silent poll and bootloader visit as test 054.
images=out/boot-bundle-test056/boot.img 08c53526d6456b701c739d451e4e15590efb7918c05ea01d01e4eed8df1e0a76 and vendor_boot.img baac5dc53c85c3d4eff696fe7bd16b1df97fa3515f02e816f810e3ce13832613 (both carry the changed DTB); init_boot.img 12b77d17 unchanged and already on the device; dtbo unchanged (c17418be)
started_utc=2026-09-22T07:22:24Z
