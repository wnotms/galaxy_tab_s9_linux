source_commit=b74f0eb + ad2a7f1 ("pogo: start the MCU the vendor's way, without GO" and its build fix)
upstream_kernel_commit=a13c140cc289c0b7b3770bce5b3ad42ab35074aa
authorization=owner asked to continue the repair ("检查仓库变化，继续修复") and asked to reduce the wait between system switches; this flash continues the authorisation recorded for tests 046-049 ("刷入测试")
hypothesis=GO is not how this part is started. Stock's probe (stm32_dev_firmware_update_mode) never sends GO: it enters the system bootloader (stm32_sysboot_mcu_validation), reads the IC version at 0x08000200 through stm32_sysboot_i2c_read, then calls stm32_sysboot_disconnect() - BOOT0 low, one NRST pulse, 150 ms - which releases the application so it runs from flash, and only then does the app protocol answer (CHECK_VERSION, GET_MODE, and STM32_CMD_ABORT if the part reports DFU). Test 049 showed the checked assumption behind GO was wrong: 0x08000000 holds a Cortex-M vector table (SP 0x200056c0, reset vector 0x0800c4a5), so Samsung's header is not there at all.
predictions=1) the IC version read must return four bytes whose last byte is 0x34, the value stock prints as mcu_fw(ic):34. 2) after the single reset of the disconnect the application must answer CHECK_VERSION, so the driver logs the model/hw/firmware/mode line instead of "no answer from the MCU". 3) if it reports mode 2 (DFU) the driver sends ABORT 0x17 and reads the mode again.
images=out/boot-bundle-test050/boot.img 31060e4d06c87f47d4ca31662e6faea9ac28d369a7fc559e2e995d08654d010d; init_boot/vendor_boot/dtbo unchanged (2fb24cd0, 828dadec, c17418be) and already on the device
started_utc=2026-09-22T06:41:17Z
