source_commit=e2db2ed ("pogo: keep gpio10 low until the driver releases the rail, and touch nothing else")
upstream_kernel_commit=a13c140cc289c0b7b3770bce5b3ad42ab35074aa
authorization=owner asked to continue the repair ("检查仓库变化，继续修复"), to reduce the wait between system switches, for the card's automatic mount to be off with only the serial console, to work from the official source in the parent folder, and directed this candidate as P0 of the plan ("优先从\"删除或改掉 pogo_supply 的提前 output-high，并禁止首次启动路径进入 bootloader\"开始"); the standing device-test authorisation recorded for tests 046-057 ("刷入测试") covers this flash
hypothesis=Two possibilities are being separated: (a) the STM32 never runs its application in mainline because it is powered before its boot pins are driven - the fixed regulator applied pogo_supply's output-high at its probe, before the pogo driver applied its own pin states, so the part came up with SWCLK (BOOT0) undriven; or (b) the power-on order is irrelevant and something else keeps the application silent.
change=one hypothesis only: pogo_supply is now output-low, so gpio10 cannot power the MCU before the driver enables the rail; and the first bring-up is the minimum stock performs here - SWCLK low, NRST untouched (its pinctrl default is output-high), rail on, then a read-only CHECK_VERSION poll at 0x2a for up to 60 s with no pin changes, no 0x51 access, no GO and no NRST pulse.  The bootloader path stays only as the fallback when the application does not answer.  The bus-silence experiment from the previous commit is reverted (its result was never captured).
success criteria=CHECK_VERSION at 0x2a answers, the driver logs the model/hw/firmware/mode line with no bootloader visit, and GET_MODE reads 1; the driver then reports the CRC and any real key event in the follow-up.
images=out/boot-bundle-test058/boot.img 417cd29c289e9f1c8621ac4783375bb1323d830902e454cd58da89e83a8a1b16 and vendor_boot.img b8f2d5ed063890b9bd3137ee694695ad900bcd84189a084d8f2df47e013b7bc5 (both carry the changed DTB); init_boot 12b77d17 unchanged; dtbo c17418be untouched
started_utc=2026-09-22T07:40:00Z
