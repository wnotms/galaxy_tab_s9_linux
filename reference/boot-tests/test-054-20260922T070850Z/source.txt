source_commit=762828a ("pogo: power the MCU up again with BOOT0 low, and make the gadget console-only")
upstream_kernel_commit=a13c140cc289c0b7b3770bce5b3ad42ab35074aa
authorization=owner asked to continue the repair ("检查仓库变化，继续修复"), asked to reduce the wait between system switches, and asked for the microSD card's automatic mount to be off with only the debug serial console left ("暂时取消对tf卡的自动挂载，只保留调试串口")
hypothesis=This port has never given the MCU a real power-on with BOOT0 low. It enables a rail that is already high from the regulator's pinctrl state (so the pin never moves), and then works the bootloader and NRST; on STM32 parts whose option bytes select the software BOOT0 an NRST pulse cannot change the boot source, so the part stays in its bootloader forever - which is exactly what every test since 046 shows (0x51 answers, 0x2a never does), while the stock stack, whose driver switches the same rail in stm32_dev_regulator(), sees the application answer. pogo_power_cycle() now drives SWCLK (BOOT0) low and holds NRST asserted while the rail goes down and up, then releases NRST.
predictions=1) the log shows "MCU powered up again with BOOT0 low". 2) the application answers CHECK_VERSION inside the patient poll that follows, so the driver reports the model/hw/firmware/mode line and no bootloader visit happens at all. 3) if it still does not answer, the bootloader visit stays as diagnostic and the log shows the same three lines as before.
also=the bundled command line is now gts9_usb_gadget=acm, so the USB gadget is the debug console only (the owner's request); vendor_boot.img changes for that reason and is flashed with boot.img.
images=out/boot-bundle-test054/boot.img efb5b992c344bb88900bf04a5f6f9fba7da09972fa3e97f2e10130a83652d1be and vendor_boot.img 6b24a94670ed66fb1c32878273751eeb6447d0d5c53ad6b5cf69f8304d2c4813; init_boot.img 12b77d17 unchanged and already on the device; dtbo unchanged (c17418be)
started_utc=2026-09-22T07:08:50Z
