source_commit=dab25d7 ("pogo: read the MCU option bytes and report bit 24") + ef271a8 ("init: leave the microSD card alone and keep the console as the only channel")
upstream_kernel_commit=a13c140cc289c0b7b3770bce5b3ad42ab35074aa
authorization=owner asked to continue the repair ("检查仓库变化，继续修复"), asked to reduce the wait between system switches, and asked for the microSD card's automatic mount to be switched off for now, keeping only the debug serial console ("暂时取消对tf卡的自动挂载，只保留调试串口")
purposes=1) carry out the owner's request: GTS9_CARD_MOUNT defaults to 0, so nothing mounts the card, nothing is exported over USB and the debug console is the only channel (the report stays in /tmp and is read with cat /tmp/bringup-report.txt). 2) read the MCU's option bytes at 0x1FFF7800 (read only) and report the word, the RDP byte and bit 24, the bit stock's stm32_target_option_update() clears on every boot and which this port has never read.
predictions=1) the boot log shows 'card mount and internal persistence disabled by GTS9_CARD_MOUNT=0' instead of any mount attempt, the USB gadget still provides the CDC-ACM console, and no card is touched. 2) the option-byte line reports either bit 24 clear (the vendor's write would do nothing, and that theory is closed) or set (the one vendor step this port has never performed, and a write becomes a decision to take).
images=out/boot-bundle-test053/boot.img 33afd56dd3bdebd29960683343033713e365f35bff1d6b5ae7a9b26f0ed8af5f and init_boot.img 12b77d174d4074c1215e1e2e39230d57b7a8ab9c0e06f3bdcef7dc5c89276e5f (the initramfs changed); vendor_boot/dtbo unchanged (828dadec, c17418be)
started_utc=2026-09-22T07:03:52Z
