source_commit=64de05f ("pogo: detect a live application passively on the announce line")
upstream_kernel_commit=a13c140cc289c0b7b3770bce5b3ad42ab35074aa
authorization=standing device-test authorisation recorded for tests 046-060 plus this round's direction to keep testing on hardware
hypothesis=After test 060 (30 s of complete silence, then 60 s of side-effect-free polling, still no answer at 0x2a) two possibilities remain: (a) the STM32 application is not running under mainline at all, or (b) it is running but does not answer this host.  The MCU asserts its own announce line (gpio75, level-low IRQ) when it has something to report - stock's byte log shows the keyboard announcing itself that way - so arming that interrupt during a window with no bus traffic at all detects a live application without influencing it.
change=connect_work arms the announce IRQ for the 30 s silent window and the handler counts interrupts without touching the bus (disable_irq_nosync after the first, so a level-low line cannot storm while unserviced).  The window ends with "silent window over; connect line X, N announce IRQ(s)".
predictions=1) N > 0 => the application is alive under mainline and the fault is in the response path (address/format/timing), which narrows the next step to the controller side; 2) N == 0 => the application is not running, and the difference from stock is in what starts it, not in how it is read.
images=out/boot-bundle-test061/boot.img ad288adaae94eab0ecec3547fd19e8798f33d3712106723fa837844767b2d0f9; vendor_boot 8d76f912, init_boot 12b77d17, dtbo c17418be unchanged
started_utc=2026-09-22T08:10:00Z
